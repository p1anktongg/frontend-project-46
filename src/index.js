const genDiff = (filepath1, filepath2, format = 'stylish') => {
  // Пока пустая функция, реализуем позже
  // format - параметр формата вывода
  return `Comparing ${filepath1} and ${filepath2} in ${format} format`;
};

export default genDiff;